#include <stdio.h>

int main() {
  int arr[4];
  for (int i = 0 ; i<4 ; i++)
  {
      printf("enter the element in array :");
    scanf("%d" , &arr[i]);
    
      }
  for (int i = 0 ; i<4 ; i++)
  {
    printf("the element in array %d is : %d \n", i , arr[i]);

    
      }
     
}